---
import Base from "../layouts/Base.astro";
---

<Base title="近况 · Ze'Yan" description="Ze'Yan 的近况（Now 页）">
  <article class="prose now reveal">
    <header class="now-head">
      <div class="now-head-accent" aria-hidden="true"></div>
      <p class="section-kicker">近况</p>
      <h1>Now</h1>
      <p class="lead">这一阵子在忙些什么——更新于 2026 年 9 月。</p>
    </header>

    <hr class="rule" />

    <div class="now-body">
      <p>最近在…</p>
      <ul class="now-list">
        <li>
          用 <strong>Astro 7</strong> 重写本站点，并持续打磨「编辑杂志风」的视觉语言与阅读体验：内容集合、站内搜索、RSS、标签与归档体系都已就位。
        </li>
        <li>
          折腾 <strong>Arch Linux + Hyprland</strong> 桌面：配平铺窗口管理、自写 dotfiles，把终端、编辑器、浏览器串成同一套肌肉记忆。
        </li>
        <li>
          在读《<em>Thinking, Fast and Slow</em>》，顺手把「系统一 / 系统二」的笔记整理进知识库，准备之后写篇随笔。
        </li>
        <li>
          在试 <strong>Hermes Agent</strong>，让它替我跑第一遍代码审查与长任务拆步，体验比想象中顺。
        </li>
        <li>
          保持每周两次长跑，目标下半年完成一场半程马拉松；周末常去江边与旧城区扫街拍长曝光。
        </li>
      </ul>
      <p>
        这个页面受 <a href="https://nownownow.com" target="_blank" rel="noopener"
          >nownownow.com</a
        > 启发，记录「此刻」而非「全部」。它会随生活节奏慢慢变化。
      </p>
    </div>
  </article>
</Base>

<style>
  .now {
    padding: 40px 0;
  }

  .now-head {
    position: relative;
    padding-left: 20px;
  }

  .now-head-accent {
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 4px;
    background: linear-gradient(180deg, var(--accent) 0%, var(--accent-2) 100%);
    border-radius: 2px;
  }

  .now-head h1 {
    font-size: clamp(3rem, 8vw, 5rem);
    margin: 8px 0 16px;
    letter-spacing: -0.03em;
    line-height: 1;
  }

  .now-body {
    margin-top: 28px;
    font-family: "Noto Serif SC", var(--serif);
    font-size: 1.05rem;
    line-height: 1.85;
  }

  .now-body :global(p) {
    margin: 0 0 1.2em;
    text-wrap: pretty;
  }

  .now-list {
    list-style: none;
    margin: 0 0 1.4em;
    padding: 0;
    counter-reset: now-counter;
  }

  .now-list li {
    position: relative;
    padding: 14px 0 14px 36px;
    border-top: 1px solid var(--border);
    counter-increment: now-counter;
  }

  .now-list li:last-child {
    border-bottom: 1px solid var(--border);
  }

  .now-list li::before {
    content: "§";
    position: absolute;
    left: 0;
    top: 14px;
    color: var(--accent);
    font-family: var(--serif);
    font-weight: 700;
    font-size: 1.1rem;
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* 编号装饰 */
  .now-list li::after {
    content: counter(now-counter, decimal-leading-zero);
    position: absolute;
    right: 0;
    top: 14px;
    font-family: var(--serif);
    font-weight: 700;
    font-size: 1.4rem;
    color: var(--border);
    user-select: none;
    letter-spacing: -0.02em;
  }
</style>
