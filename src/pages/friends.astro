---
import Base from "../layouts/Base.astro";

// 在此添加友链：name 站点名 / url 链接 / desc 一句简介
const friends: { name: string; url: string; desc: string }[] = [
  // {
  //   name: "xxx",
  //   url: "https://example.com",
  //   desc: "一句话描述",
  // },
];
---

<Base
  title="友链 · Ze'Yan"
  description="Ze'Yan 的友情链接"
>
  <article class="prose friends reveal">
    <header class="friends-head">
      <p class="section-kicker">友链</p>
      <h1>友情链接</h1>
      <p class="lead">
        我欣赏并关注的一些站点。想做友链，欢迎通过页面底部的邮箱联系我。
      </p>
    </header>

    <hr class="rule" />

    {
      friends.length > 0 ? (
        <div class="friends-grid">
          {
            friends.map((f) => (
              <a class="friend-card" href={f.url} target="_blank" rel="noopener noreferrer">
                <span class="friend-name">{f.name}</span>
                <span class="friend-desc">{f.desc}</span>
              </a>
            ))
          }
        </div>
      ) : (
        <p class="friends-empty">
          友链整理中，敬请期待。
        </p>
      )
    }

    <section class="friends-how">
      <h2>申请友链</h2>
      <p>
        想交换友链，欢迎发邮件到
        <a href="mailto:hi@laizeyan.github.io">hi@laizeyan.github.io</a>
        。请附上你的站点名称、链接与一句简介。
      </p>
      <ul class="friends-rules">
        <li>内容有长期价值的站点优先——技术笔记、写作、或你真正热爱的东西。</li>
        <li>站点可正常访问，无恶意或违规内容。</li>
        <li>先放上本站友链，再来找我，这样更快通过。</li>
      </ul>
    </section>
  </article>
</Base>

<style>
  .friends {
    padding: 40px 0;
  }

  .friends-head h1 {
    font-size: clamp(2.5rem, 8vw, 4.5rem);
    margin: 8px 0 16px;
    letter-spacing: -0.02em;
  }

  .friends-head .lead {
    margin: 0;
    max-width: 560px;
  }

  .friends-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: 16px;
    margin-top: 32px;
  }

  .friend-card {
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding: 18px 20px;
    background-color: var(--bg-elevated);
    border: 1px solid var(--border);
    border-left: 3px solid var(--accent);
    border-radius: 4px;
    text-decoration: none;
    transition: border-color 0.2s ease, transform 0.2s ease;
  }

  .friend-card:hover {
    border-color: var(--accent);
    transform: translateY(-2px);
  }

  .friend-name {
    font-family: var(--serif);
    font-weight: 600;
    font-size: 1.15rem;
    color: var(--text);
  }

  .friend-card:hover .friend-name {
    color: var(--accent);
  }

  .friend-desc {
    color: var(--text-muted);
    font-size: 0.9rem;
    line-height: 1.6;
  }

  .friends-empty {
    margin-top: 40px;
    color: var(--text-muted);
    font-size: 1.05rem;
  }

  .friends-how {
    margin-top: 48px;
    padding: 24px 28px;
    background-color: var(--bg-elevated);
    border: 1px solid var(--border);
    border-left: 3px solid var(--accent);
    border-radius: 4px;
  }

  .friends-how h2 {
    font-size: 1.3rem;
    margin: 0 0 10px;
  }

  .friends-how p {
    margin: 0 0 14px;
    color: var(--text-muted);
    line-height: 1.7;
  }

  .friends-rules {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 8px;
    font-size: 0.95rem;
    color: var(--text-muted);
    line-height: 1.6;
  }

  .friends-rules li {
    position: relative;
    padding-left: 20px;
  }

  .friends-rules li::before {
    content: "§";
    position: absolute;
    left: 0;
    color: var(--accent);
    font-family: var(--serif);
    font-weight: 700;
  }
</style>
