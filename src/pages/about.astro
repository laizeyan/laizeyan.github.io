---
import Base from "../layouts/Base.astro";
---

<Base
  title="关于 · Ze'Yan"
  description="关于 Ze'Yan：AI 工具驱动的全栈工程师，关注智能体工作流、Linux 与 Web 开发"
>
  <article class="prose about reveal">
    <header class="about-head">
      <p class="section-kicker">关于</p>
      <h1>Ze'Yan</h1>
      <p class="lead">
        一名以 AI 工具驱动的全栈工程师，热衷于把复杂的想法做成简洁、可维护、可复现的系统。
      </p>
    </header>

    <hr class="rule" />

    <div class="about-body">
      <p>
        我关注的是一条「工具驱动」的路线：用智能体与脚本把重复劳动让渡出去，把判断与架构留给自己。
        从 Astro 静态站点到 Python 后端，从多 Agent 协作的编码流程到跨机器一键还原的 dotfiles，
        这里记录我的项目、思考与对工作流的持续打磨。
      </p>
      <p>
        工作之外，我喜欢阅读、摄影与长跑，相信长期主义能把小事做成作品。
        如果你对我的内容感兴趣，欢迎通过页面底部的社交链接与我联系。
      </p>

       <h2>技能</h2>
       <ul class="skills">
         <li><span class="skills-k">AI/Agent</span> opencode、Codex、Hermes、多 Agent 协作工作流</li>
         <li><span class="skills-k">前端</span> Astro、TypeScript、React、现代 CSS</li>
         <li><span class="skills-k">后端</span> Python、Node.js、API 与数据库设计</li>
         <li><span class="skills-k">工具链</span> Neovim、Linux/Hyprland、dotfiles、Git、CI/CD</li>
       </ul>

       <h2>关注领域</h2>
       <div class="focus">
         <article class="focus-col">
           <h3>AI 驱动的开发工作流</h3>
           <p>
             用智能体做代码审查、长任务拆步，并搭建 planner / reviewer 等多角色协作流程。相信「人机协作」能同时提升速度与质量。
           </p>
         </article>
         <article class="focus-col">
           <h3>一键还原的开发环境</h3>
           <p>
             用 Git 管理整套 dotfiles，重装系统后跑一个脚本即可恢复。环境可复现，是稳定产出的前提。
           </p>
         </article>
         <article class="focus-col">
           <h3>克制的视觉系统</h3>
           <p>
             用留白、字阶与一条发丝线讲完故事。关注排版节奏与可读性，而不是堆砌装饰——这也是本站「编辑杂志风」的由来。
           </p>
         </article>
       </div>
    </div>
  </article>
</Base>

<style>
  .about {
    padding: 40px 0;
  }

  .about-head h1 {
    font-size: clamp(2.5rem, 8vw, 4.5rem);
    margin: 8px 0 16px;
    letter-spacing: -0.02em;
  }

  .about-body {
    margin-top: 28px;
    font-family: "Noto Serif SC", var(--serif);
    font-size: 1.05rem;
    line-height: 1.85;
  }

  .about-body :global(p) {
    margin: 0 0 1.2em;
    text-wrap: pretty;
  }

  .about-body :global(h2) {
    font-size: 1.6rem;
    margin: 1.8em 0 0.6em;
  }

  .skills {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: 12px;
  }

  .skills li {
    background-color: var(--bg-elevated);
    border: 1px solid var(--border);
    border-left: 3px solid var(--accent);
    border-radius: 4px;
    padding: 14px 16px;
    font-size: 0.95rem;
  }

  .skills-k {
    display: block;
    font-family: var(--sans);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    font-size: 0.7rem;
    font-weight: 600;
    color: var(--accent);
    margin-bottom: 4px;
  }

  .focus {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 22px;
    margin-top: 8px;
  }

  .focus-col {
    border-top: 2px solid var(--accent);
    padding-top: 14px;
  }

  .focus-col h3 {
    font-size: 1.2rem;
    margin: 0 0 8px;
  }

  .focus-col p {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.95rem;
    line-height: 1.7;
  }
</style>
